<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
     <a href="{% url 'index' %}" class="navbar-brand">Rhixescans</a>
    <button
    
      class="navbar-toggler collapsed"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarColor02"
      aria-controls="navbarColor02"
      aria-expanded="true"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse" id="navbarColor02" style="">
      <ul class="navbar-nav me-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/search">Search</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{% url 'comics' %}">Comics</a>
        </li>
        
        {% if request.user.is_authenticated %}
        <li class="nav-item dropdown">
          <a class="" href="#">Users</a>
          
        </li>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{% url "user-profile" user.id %}">Dashboard</a>
          <a class="dropdown-item" href="{% url "update-user" user.id %}">Update Info</a>
          <a class="dropdown-item" href="{% url 'bookmark_list' %}">Bookmarks</a>
          <a class="dropdown-item" href='{% url 'logout' %}'>Logout</a>
        </div>
        {% else %}
        <li class="nav-item">
           <a class="nav-link" href='{% url 'login' %}'>Login</a>
        </li>
         <li class="nav-item">
          <a class="nav-link" href='{% url 'signup' %}'>Register</a>
        </li>
        
        {% endif %}
      </ul>
      <form class="d-flex form-inline my-2 my-lg-0"  method="GET" action='/search/'>
            <input type='text'  class="form-control me-sm-2" name='q' placeholder='Search' aria-label='Search' required/>
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
          Search
        </button>
          </form>
      
    </div>
  </div>
  
</nav>
 {% if messages %}
      <ul class="messages">
        {% for message in messages %}
        <li>{{ message }}</li>
        {% endfor %}
      </ul>
      {% endif %} 